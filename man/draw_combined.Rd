% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw.R
\name{draw_combined}
\alias{draw_combined}
\title{Draw a matriks and its response list on the same device}
\usage{
draw_combined(
  mat,
  resp,
  resp_height = 0.3,
  frow = NULL,
  mat_args = list(hide = TRUE),
  resp_args = list()
)
}
\arguments{
\item{mat}{A matriks object to be drawn in the upper panel}

\item{resp}{A responses object to be drawn in the lower panel}

\item{resp_height}{numeric, proportion of the device height allocated to the
response list. Must be between 0 and 1. Default is 0.3.}

\item{frow}{numeric vector of length 2 (nrow, ncol), layout of the response
options panel. Default is c(1, 4).}

\item{mat_args}{list, additional arguments passed to \code{draw(mat, ...), default hide=TRUE}}

\item{resp_args}{list, additional arguments passed to \code{draw(resp, ...)}}
}
\value{
A ggplot/cowplot object rendered to the current device
}
\description{
Draws a matriks in the upper portion of the device and its response list
in the lower portion. Uses \code{cowplot} to capture each base R plot
independently and compose them, avoiding layout conflicts between the two
draw calls.
}
\examples{
my_mat <- mat_apply(cof(circle(), luck(), pacman()), "shade", "shape")
my_resp <- response_list(my_mat)
draw_combined(my_mat, my_resp, resp_height = 0.3)
}
